pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
plotly>=5.18
streamlit>=1.30
langchain-core>=0.3
# Provedores LLM (opcionais — a POC roda 100% offline com LLM_PROVIDER=mock)
# pip install langchain-openai -> para LLM_PROVIDER=openai
# pip install langchain-aws    -> para LLM_PROVIDER=bedrock
