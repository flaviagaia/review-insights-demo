# Versões pinadas em faixas estreitas: reprodutibilidade > conveniência.
# Testado com as versões mínimas de cada faixa (CI executa a cada push).
pandas>=2.3,<3
numpy>=2.2,<3
scikit-learn>=1.7,<2
matplotlib>=3.10,<4
plotly>=6.9.0,<7
streamlit>=1.50,<2
langchain-core>=1.4,<2
pyarrow>=24.0,<25
joblib>=1.5,<2

# Provedores LLM (opcionais — a POC roda 100% offline com LLM_PROVIDER=mock)
# pip install "langchain-openai>=1,<2"  -> para LLM_PROVIDER=openai
# pip install "langchain-aws>=1,<2"     -> para LLM_PROVIDER=bedrock
